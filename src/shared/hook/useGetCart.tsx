export const useGetCart = () => {
  // const { data: carts } = useSelector((state: RootState) => state.cart)
  // const {
  //   token,
  //   userInfo: { partner_id },
  // } = useSelector((state: RootState) => state.user)
  // if (!token) return { length: 0, totalMoney: 0, carts: [] }
  // if (carts.length === 0) return { length: 0, carts: [], totalMoney: 0 }
  // const totalMoney = carts.reduce((a, b) => b.price * b.quantity + a, 0)
  // return {
  //   length: carts.length,
  //   totalMoney,
  //   carts,
  // }
}

export default useGetCart
